# NEWARONENGINE 빌드 가이드

## 현재 상태
- ✅ DirectSound → FMOD 변환 완료
- ✅ 프로젝트 구조 설정 완료
- ✅ 더미 FMOD 구현으로 컴파일 준비 완료

## 빌드 단계

### 1. Visual Studio에서 빌드
1. `NEWARONENGINE.sln` 열기
2. **Debug x64** 구성 선택
3. **솔루션 빌드** (Ctrl+Shift+B)

### 2. FMOD SDK 설치 (실제 오디오 기능 사용 시)
1. https://www.fmod.com/download 에서 FMOD Core API 다운로드
2. 다음 파일들을 복사:

**헤더 파일을 `ThirdParty/FMOD/inc/`에:**
- fmod.hpp (기존 더미 파일 덮어쓰기)
- fmod.h
- fmod_common.h (기존 더미 파일 덮어쓰기)
- fmod_errors.h (기존 더미 파일 덮어쓰기)

**라이브러리를 `ThirdParty/FMOD/lib/`에:**
- fmod_vc.lib (Release용)
- fmodL_vc.lib (Debug용)

**DLL을 `x64/Debug/`에:**
- fmodL.dll (Debug용)
- fmod.dll (Release용)

### 3. 실행
- `x64/Debug/Game.exe` 실행
- 처음에는 더미 FMOD로 컴파일되므로 소리가 나지 않음
- 실제 FMOD SDK 설치 후 다시 빌드하면 소리 재생 가능

## 조작법
- **WASD/방향키**: 플레이어 이동 및 점프
- **T**: 오디오 재생 테스트
- **M**: 오디오 정지
- **L**: 루프 모드 토글
- **E**: 레벨 에디터 토글
- **ESC**: 종료

## 문제 해결
- 빌드 오류 시: Visual Studio 2022, Windows SDK 설치 확인
- 링크 오류 시: FMOD 라이브러리 경로 확인
- 실행 오류 시: FMOD DLL 파일이 실행 폴더에 있는지 확인

## 주요 변경점
1. **AudioManager**: FMOD::System 기반으로 완전 재작성
2. **AudioClip**: FMOD::Sound 사용, 다양한 포맷 지원
3. **AudioSource**: FMOD::Channel 기반 실시간 제어
4. **TestApp**: FMOD 오디오 테스트 기능 추가